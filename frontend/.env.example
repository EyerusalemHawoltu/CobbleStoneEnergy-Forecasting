# Backend API URL
# In local dev this is proxied via vite.config.ts — leave as localhost.
# In production (Vercel), set this to your Railway backend URL.
# Example: VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=http://localhost:8000
